// ── Splinter 4x4 Build Board — configuration ────────────────────────────────
//
// The app works with NO setup at all: leave these as-is and the board stores
// jobs in your browser (localStorage). Good for trying it out on one device.
//
// To turn on TRUE LIVE TRACKING (customers see updates the moment you advance a
// stage, from any device), create a free Supabase project and paste the two
// values below. Setup steps are in README.md (about 15 minutes).
//
// Find these in Supabase → Project Settings → API.
window.SUPABASE_URL = "YOUR_SUPABASE_URL";        // e.g. https://abcd1234.supabase.co
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";

// The public base URL where THIS app is hosted (used to build customer links).
// Leave blank to auto-detect from the current address bar.
window.PUBLIC_BASE_URL = "";
